import {
  parseClient,
  getAlgoName,
  toRegistrationInfo,
  parseAuthenticator,
  parseAuthentication,
  parseRegistration,
} from './parsers/index';

export const parsers = {
  parseClient,
  getAlgoName,
  toRegistrationInfo,
  parseAuthentication,
  parseAuthenticator,
  parseRegistration,
};
