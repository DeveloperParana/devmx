import {
  parseClient,
  getAlgoName,
  toRegistrationInfo,
  parseAuthenticator,
  parseAuthentication,
} from './parsers/index';

export const parsers = {
  parseClient,
  getAlgoName,
  toRegistrationInfo,
  parseAuthentication,
  parseAuthenticator,
};
